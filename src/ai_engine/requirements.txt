# DraftBetter AI Engine Dependencies
# 
# Installation:
#   pip install -r requirements.txt
#
# For ROCm (AMD GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm5.6
#
# For CUDA (NVIDIA GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For MPS (Apple Silicon):
#   pip install torch torchvision (standard install works)

# Core ML Framework
torch>=2.1.0

# Async HTTP Client
aiohttp>=3.9.0

# Database (built into Python, but listed for clarity)
# sqlite3 is part of Python standard library

# Data Processing
numpy>=1.24.0

# Progress bars (optional but nice)
tqdm>=4.66.0

# Configuration
python-dotenv>=1.0.0

# Typing extensions for older Python
typing-extensions>=4.8.0
